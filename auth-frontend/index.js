const login_URL = 'http://localhost:8000/users/login'

const doShit = () => {
  // write some frickin code that grabs the username and password from the form and posts to the api login endpoint
  // don't forget to prevent default behavior of form submission (event.preventDefault())
  // if jwt is created and sent back, store token in localStorage and change location to success page
  // otherwise post error message in index.html
}

document.addEventListener('DOMContentLoaded', doShit)
