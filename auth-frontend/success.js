const token_URL = 'http://localhost:8000/users/token'

const greatSuccess = () => {
  // write some frickin code that checks to see if a token is stored in local localStorage
  // if not redirect user back too login page
  // otherwise post token to token URL to check validity
  // if valid show sucess message in dom on sucess.html page
}

document.addEventListener('DOMContentLoaded', greatSuccess)
